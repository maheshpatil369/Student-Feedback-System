# Root dependencies
/node_modules
# package-lock.json (and yarn.lock, pnpm-lock.yaml) should typically be committed
# to ensure reproducible installs across environments.
# If you have a specific reason to ignore a root lock file, you can add it here,
# e.g., /package-lock.json
# (The line above that was previously /package-lock.json has been commented out)
node_modules

# Environment files
.env
.env.*.local

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Testing & Coverage Reports
coverage/
*.lcov

# OS files
.DS_Store
Thumbs.db

# IDEs
.vscode/
.idea/

# TypeScript build cache
*.tsbuildinfo

# Vite / React frontend
/frontend/node_modules
/frontend/dist
/frontend/.vite

# Environment files in frontend
/frontend/.env
/frontend/.env.*.local

# Vite specific
/frontend/.cache

# Node backend
/backend/node_modules

# Build files (if any)
/backend/dist

# Environment files in backend
/backend/.env
/backend/.env.*.local

